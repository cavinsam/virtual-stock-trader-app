# Python requirements for Ansible deployment
# Install with: pip install -r requirements.txt

# Core Ansible
ansible>=2.9.0,<3.0.0

# Required Python packages
pyyaml>=5.4
requests>=2.26.0
cryptography>=3.4.8

# Optional but recommended
jinja2>=3.0.0
netaddr>=0.8.0

# Ansible collections (installed via ansible-galaxy, but can be pinned here)
# community.docker>=1.10.0
# kubernetes.core>=2.3.0
